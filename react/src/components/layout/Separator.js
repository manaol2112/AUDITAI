// Separator.jsx

import React from 'react';

const Separator = () => {
    const separatorStyle = {
        borderTop: '1px solid #cccccc', // Example color and thickness
        margin: '20px 0', // Optional: Adjust margin as per your layout
    };

    return <div style={separatorStyle}></div>;
};

export default Separator;
